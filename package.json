{
  "name": "q-view",
  "version": "0.1.0",
  "private": true,
  "description": "GenAI Conversation Viewer - TypeScript tool for visualizing JSON conversation history",
  "scripts": {
    "build": "pnpm -r build",
    "build:web": "pnpm --filter website build",
    "dev": "pnpm --filter website dev",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean",
    "deploy:infra": "pnpm --filter infrastructure deploy"
  },
  "devDependencies": {
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.15.0"
}
